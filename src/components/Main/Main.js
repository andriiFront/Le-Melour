import React from 'react';
import { Hero } from '../Hero';
import './Main.scss';

export const Main = () => (
  <main className="main">
    <section className="main__hero">
      <Hero />
    </section>
  </main>
);
